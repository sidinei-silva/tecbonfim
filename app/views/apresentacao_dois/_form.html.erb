<%= form_with(model: apresentacao_doi, local: true) do |form| %>
  <% if apresentacao_doi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apresentacao_doi.errors.count, "error") %> prohibited this apresentacao_doi from being saved:</h2>

      <ul>
      <% apresentacao_doi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_area :descricao %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
